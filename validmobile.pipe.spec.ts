import { ValidmobilePipe } from './validmobile.pipe';

describe('ValidmobilePipe', () => {
  it('create an instance', () => {
    const pipe = new ValidmobilePipe();
    expect(pipe).toBeTruthy();
  });
});
